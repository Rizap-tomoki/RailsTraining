<h1>従業員管理サイト</h1>
<%= link_to "従業員情報登録", new_admin_user_path %><br/>
<%= link_to "部署名一覧", admin_departments_path %><br/>
<%= link_to "スキル名一覧", admin_skills_path %>

<ul>
  <% @users.each do |user| %>
    <li>
     <%= link_to user.name, admin_user_path(user) %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>